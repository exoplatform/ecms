<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.exoplatform.ecms</groupId>
    <artifactId>ecms-apps</artifactId>
    <version>4.0.0.Alpha1-relooking-SNAPSHOT</version>
  </parent>
  <artifactId>ecms-apps-resources-wcm</artifactId>
  <packaging>war</packaging>
  <name>eXo WCM Resources</name>
  
  <build>
    <finalName>eXoWCMResources</finalName>
    <plugins>
      <plugin>
         <groupId>org.apache.maven.plugins</groupId>
         <artifactId>maven-dependency-plugin</artifactId>
         <executions>
            <execution>
               <id>unpack</id>
               <phase>compile</phase>
               <goals>
                  <goal>unpack</goal>
               </goals>
               <configuration>
                  <artifactItems>
                     <artifactItem>
                        <groupId>org.exoplatform.platform-ui</groupId>
                        <artifactId>platform-ui-web-eXoResources</artifactId>
                        <type>war</type>
                        <overWrite>false</overWrite>
                        <outputDirectory>${project.build.directory}/${project.build.finalName}</outputDirectory>
                        <includes>skin/less/mixins.less,skin/less/variables.less</includes>
                     </artifactItem>
                  </artifactItems>
               </configuration>
            </execution>
         </executions>
      </plugin>
    <!-- Plugin to create css from less File to be able to build the skin -->
      <plugin>
         <groupId>org.lesscss</groupId>
         <artifactId>lesscss-maven-plugin</artifactId>
         <executions>
            <execution>
               <id>administration</id>
               <phase>package</phase>
               <configuration>
                  <sourceDirectory>${project.build.directory}/${project.build.finalName}/skin/less</sourceDirectory>
                  <outputDirectory>${project.build.directory}/${project.build.finalName}/skin/css</outputDirectory>
                  <compress>false</compress>
                  <includes>
		            <include>ecms-resources-wcmskin.less</include>
            		<include>ecms-resources-dmsskin.less</include>
                  </includes>
               </configuration>
               <goals>
                  <goal>compile</goal>
               </goals>
            </execution>
         </executions>
      </plugin>
    </plugins>
  </build>
</project>
